export const enum EnvironmentType {
    production = 'production',
    local = 'local',
    testing = 'testing',
}

export const enum PreferredApps {
    'om' = 'om',
    'essentials' = 'essentials',
}
